import QtQuick 2.0
import CLine 1.0

CLine {
    id: root

    z: -1
    color: /*Config.lineColorDarkGreen()*/"red"
    radius: /*Config.radiusMedium*/5
    penWidth: /*Math.round(2 * Config.scale)*/2
}
